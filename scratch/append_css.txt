
/* --- Loading Screen Animation --- */
.loading-overlay {
    position: fixed;
    inset: 0;
    background: var(--paper);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.4s ease;
}

.loading-overlay.active {
    opacity: 1;
    pointer-events: all;
}

.load-line {
    width: 100%;
    height: 1.5px;
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
}

.load-line::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%) scaleX(0);
    width: 100%;
    height: 1.5px;
    background: var(--ink);
    opacity: 0.2;
    transform-origin: center;
}

.loading-overlay.active .load-line::before {
    animation: lineGrow 0.6s ease forwards;
}

@keyframes lineGrow {
    0% { transform: translateY(-50%) scaleX(0); }
    100% { transform: translateY(-50%) scaleX(1); }
}

.load-text {
    font-family: 'Fraunces', serif;
    font-size: 48px;
    font-weight: 700;
    color: var(--ink);
    position: absolute;
    left: 100vw;
    background: var(--paper);
    padding: 0 20px;
}

.load-text em {
    font-style: italic;
    color: var(--verm);
}

.loading-overlay.active .load-text {
    /* Slide in from right (0%), slow down in middle (40-60%), slide out left (100%) */
    animation: slideName 2.4s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    animation-delay: 0.3s;
}

@keyframes slideName {
    0% {
        left: 100vw;
    }
    40% {
        left: 50vw;
        transform: translateX(-50%);
    }
    65% {
        left: 45vw; /* Slight drift while reading */
        transform: translateX(-50%);
    }
    100% {
        left: -50vw;
        transform: translateX(-50%);
    }
}
